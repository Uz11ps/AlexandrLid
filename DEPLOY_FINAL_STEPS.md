# Финальные шаги после деплоя

## ✅ Что уже сделано:
- Docker установлен и работает
- Проект клонирован из GitHub
- Все контейнеры собраны и запущены
- База данных инициализирована
- Администратор CRM создан
- Nginx настроен для проксирования

## 🔧 Что нужно доделать:

### 1. Настройка SSL сертификата (Let's Encrypt)

Выполните на сервере:

```bash
certbot --nginx -d momentumtrading.ru -d www.momentumtrading.ru --email vvlad1001@gmail.com --agree-tos --non-interactive
```

Или интерактивно:

```bash
certbot --nginx -d momentumtrading.ru -d www.momentumtrading.ru
# Введите email: vvlad1001@gmail.com
# Согласитесь с условиями
```

### 2. Проверка работы системы

#### Проверка доступности сервисов:

```bash
# Проверка CRM Backend API
curl http://localhost:3001/health

# Проверка CRM Frontend
curl http://localhost:3000

# Проверка логов бота
docker compose logs bot | tail -20

# Проверка логов CRM Backend
docker compose logs crm-backend | tail -20
```

#### Проверка через браузер:

1. Откройте https://momentumtrading.ru
2. Должна открыться страница входа в CRM
3. Войдите с учетными данными:
   - Email: `admin@momentumtrading.ru`
   - Пароль: тот, который вы указали при создании администратора

### 3. Проверка работы Telegram бота

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Бот должен ответить приветственным сообщением
4. Проверьте, что лид создался в CRM (войдите в CRM и проверьте список лидов)

### 4. Настройка автоматического обновления SSL

Certbot автоматически настроит обновление сертификата. Проверьте:

```bash
systemctl status certbot.timer
```

### 5. Настройка бэкапов (рекомендуется)

Создайте скрипт для бэкапа базы данных:

```bash
cat > /opt/AlexandrLid/backup-db.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/opt/backups"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR

docker compose exec -T postgres pg_dump -U postgres telegram_bot_db > $BACKUP_DIR/backup_$DATE.sql

# Удаление старых бэкапов (старше 7 дней)
find $BACKUP_DIR -name "backup_*.sql" -mtime +7 -delete

echo "Бэкап создан: backup_$DATE.sql"
EOF

chmod +x /opt/AlexandrLid/backup-db.sh

# Настройка ежедневного бэкапа в 2:00
(crontab -l 2>/dev/null; echo "0 2 * * * /opt/AlexandrLid/backup-db.sh >> /var/log/crm-backup.log 2>&1") | crontab -
```

### 6. Мониторинг системы

#### Просмотр статуса всех контейнеров:

```bash
docker compose ps
```

#### Просмотр использования ресурсов:

```bash
docker stats
```

#### Просмотр логов:

```bash
# Все логи
docker compose logs -f

# Логи конкретного сервиса
docker compose logs -f bot
docker compose logs -f crm-backend
docker compose logs -f crm-frontend
docker compose logs -f postgres
```

### 7. Обновление проекта (когда появятся изменения в GitHub)

```bash
cd /opt/AlexandrLid
git pull
docker compose down
docker compose build
docker compose up -d
```

## 🔒 Безопасность

### Изменение пароля root (рекомендуется):

```bash
passwd
```

### Настройка firewall:

```bash
apt install ufw -y
ufw allow 22/tcp   # SSH
ufw allow 80/tcp   # HTTP
ufw allow 443/tcp  # HTTPS
ufw enable
ufw status
```

## 📝 Полезные команды

### Перезапуск сервисов:

```bash
# Перезапуск всех сервисов
docker compose restart

# Перезапуск конкретного сервиса
docker compose restart bot
docker compose restart crm-backend
docker compose restart crm-frontend
```

### Остановка/запуск:

```bash
# Остановка всех сервисов
docker compose down

# Запуск всех сервисов
docker compose up -d
```

### Вход в контейнер базы данных:

```bash
docker compose exec postgres psql -U postgres -d telegram_bot_db
```

### Создание дополнительных администраторов CRM:

```bash
docker compose exec crm-backend node scripts/create-admin.js email@example.com password "Manager Name"
```

## 🐛 Решение проблем

### Если контейнеры не запускаются:

```bash
# Просмотр детальных логов
docker compose logs --tail=100

# Пересборка без кэша
docker compose build --no-cache
docker compose up -d
```

### Если база данных не подключается:

```bash
# Проверка статуса PostgreSQL
docker compose exec postgres pg_isready -U postgres

# Проверка подключения
docker compose exec postgres psql -U postgres -d telegram_bot_db -c "SELECT 1;"
```

### Если порты заняты:

```bash
# Проверка занятых портов
netstat -tulpn | grep :3000
netstat -tulpn | grep :3001
netstat -tulpn | grep :5433

# Остановка процессов (если нужно)
kill -9 <PID>
```

## ✅ Финальная проверка

После выполнения всех шагов проверьте:

1. ✅ https://momentumtrading.ru открывается
2. ✅ Можно войти в CRM с учетными данными администратора
3. ✅ Telegram бот отвечает на команды
4. ✅ Лиды создаются в CRM при регистрации пользователей в боте
5. ✅ Можно отправлять сообщения пользователям через CRM
6. ✅ SSL сертификат работает (зеленый замочек в браузере)

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker compose logs -f`
2. Проверьте статус контейнеров: `docker compose ps`
3. Проверьте переменные окружения в `.env` файле
4. Убедитесь, что все порты свободны

